[project]
name = "loc-example-one-shot-job"
version = "0.0.0"
description = "Example: submit a one-shot job via the handoff-mode SDK."
requires-python = ">=3.10"
dependencies = [
  "livepeer-open-clearinghouse-sdk",
  "eth-keys>=0.7",
  "eth-hash[pycryptodome]>=0.7",
]

[tool.uv]
package = false

[tool.uv.sources]
livepeer-open-clearinghouse-sdk = { workspace = true }
