[project]
name = "loc-example-streaming-http"
version = "0.0.0"
description = "Example: extensible paid-session/v1 with authoritative HTTP top-up."
requires-python = ">=3.10"
dependencies = ["livepeer-open-clearinghouse-sdk"]

[tool.uv]
package = false

[tool.uv.sources]
livepeer-open-clearinghouse-sdk = { workspace = true }
