[project]
name = "loc-example-streaming-ws"
version = "0.0.0"
description = "Example: paid-session/v1 with an optional events WebSocket."
requires-python = ">=3.10"
dependencies = [
  "livepeer-open-clearinghouse-sdk",
  "eth-keys>=0.7",
  "eth-hash[pycryptodome]>=0.7",
]

[tool.uv]
package = false

[tool.uv.sources]
livepeer-open-clearinghouse-sdk = { workspace = true }
